# Copy this file to `.env` and fill in your own values.
# Note: anything prefixed VITE_ is bundled into the client — do not put real
# secrets here. This password is a soft gate for the editor UI, not security.

VITE_ADMIN_PASSWORD=changeme
